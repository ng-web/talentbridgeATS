# Technical Processor and Subprocessor Register

This register records application/operations facts, not contract conclusions. Legal entity names, processor roles, contract status, locations, and transfer safeguards require confirmation by Kairox.

| Service | Technical purpose and configuration evidence | App data likely processed | Applicant content expectation | Kairox facts to obtain |
|---|---|---|---|---|
| DigitalOcean | Candidate production infrastructure provider referenced by the deployment documentation; production uses Docker services for Caddy, Nginx, Laravel, MySQL, queue, and scheduler. Confirm the actual host behind the `DEPLOY_HOST` secret. | If confirmed as host: application/database records, private uploads, logs, backups, network metadata, and secrets held on the server. | Yes, only if DigitalOcean is confirmed as the production host. | Exact legal entity/service, regions and backup locations, subprocessors, retention, DPA, breach SLA, deletion, transfer mechanism, access/support model. |
| Cloudflare | Production edge/DNS/proxy observed for `portal.kairoxexchange.com`; origin configuration is not stored in this repository. | IP/request metadata, headers, URLs, security events, and cached content depending on configuration. | Private applicant documents must not be deliberately cached; authenticated responses need cache review. | Account/legal entity, enabled products, processing countries, cache/log settings, subprocessors, retention, DPA, breach SLA, deletion, transfer mechanism. |
| Resend | Laravel mail transport (`config/mail.php`, `config/services.php`) using `RESEND_API_KEY`. | Recipient/sender addresses, subject, notification body, delivery metadata. | No applicant documents or sensitive-document details; minimized application/payment-assistance event data only. | Legal entity, sending region, message/log retention, subprocessors, DPA, breach SLA, deletion, transfer mechanism. |
| WiPay | Hosted checkout and callback (`config/services.php`, payment services/controllers). | Account/order/payment identifiers, amount/currency, payer name/email and possibly phone required by current gateway request, transaction reference, callback status. | No applicant documents. New local persistence/logging must exclude raw provider payloads, hashes, secrets, and unnecessary customer details. | Exact legal entity, processing countries, PCI responsibilities, retained payer fields, subprocessors, DPA/merchant terms, breach SLA, deletion, transfer mechanism. |
| GitHub | Source control, dependencies, repository secrets, and manual Actions deployment (`.github/workflows/deploy.yml`). | Source/configuration, commit identities, workflow metadata, encrypted deployment secrets. Production database backup content should remain on the host and must not be uploaded to Actions. | Applicant content must not be committed, placed in issues/actions output, or stored as an artifact. | Organization/legal entity, repository visibility, access list, Actions log/artifact retention, subprocessors, DPA, breach SLA, deletion, transfer mechanism. |

## Maintenance rule

Likeslocale updates technical facts when architecture changes. Kairox approves each vendor role, legal terms, cross-border mechanism, and whether the service may process each data category.
